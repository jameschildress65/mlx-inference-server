# Vision-specific dependencies for venv-vision
# Install with: pip install -r requirements-vision.txt

# Vision model inference
mlx-vlm>=0.3.9

# Image processing
Pillow>=10.0.0

# Transformers (version constrained for mlx-vlm compatibility)
transformers>=4.44.0,<5.0

# PyTorch dependencies (required by mlx-vlm's AutoVideoProcessor)
torch>=2.0.0
torchvision>=0.15.0

# Server dependencies (needed in both envs)
setproctitle>=1.3.3
pyyaml>=6.0.0
posix_ipc>=1.0.0
